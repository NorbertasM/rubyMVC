<%= form_with(model: preview_status) do |form| %>
  <% if preview_status.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(preview_status.errors.count, "error") %> prohibited this preview_status from being saved:</h2>

      <ul>
        <% preview_status.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :channel_id, style: "display: block" %>
    <%= form.number_field :channel_id %>
  </div>

  <div>
    <%= form.label :preview_id, style: "display: block" %>
    <%= form.number_field :preview_id %>
  </div>

  <div>
    <%= form.label :valid_until, style: "display: block" %>
    <%= form.datetime_field :valid_until %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
